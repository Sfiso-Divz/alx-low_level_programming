#include "main.h"

/**
 * _abs - Computes absolute value of integer
 * @t: The inter to be computed
 * Return: The absolute value of integer
 */

int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
